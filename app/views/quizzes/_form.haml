= simple_form_for @quiz do |f|

  = f.error :base

  = f.input :name

  %p Predviđeni razredi:
  = f.input :grades, collection: grades, as: :check_boxes, label: false

  = buttons(f) do |b|
    = b.cancel_button "Odustani", quizzes_path
    = b.submit_button

  - if @quiz.new_record?
    = notice "Kviz zasad neće biti vidljiv učenicima. Kasnije ga možete učiniti vidljivim kada bude spreman."
