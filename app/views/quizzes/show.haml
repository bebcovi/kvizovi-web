- @title = "Uređivanje kviza"

%p= back_button "Natrag", quizzes_path

%section
  %h1 Info

  %dl#info
    %dt Naziv
    %dd #{@quiz.name}
    %dt Razred
    %dd #{@quiz.grade}.
    %dt Vidljiv?
    %dd #{@quiz.activated? ? "Da" : "Ne"}

  .controls
    = edit_button "Izmjeni", edit_quiz_path(@quiz)

%section
  %h1 Pitanja

  %ol#questions
    = list_of @quiz.questions do |question|
      #{link_to question, quiz_question_path(@quiz, question)}
      .controls
        #{edit_button "", edit_quiz_question_path(@quiz, question)}
        #{delete_button "", quiz_question_path(@quiz, question)}

  %p= add_button "Novo pitanje", new_quiz_question_path(@quiz)
