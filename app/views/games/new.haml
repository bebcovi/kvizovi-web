- @title = "Nova igra"

- content_for :info do
  :markdown
    # Dobrodošli

    Ovo je aplikacija za ispitivanje znanja iz lektire. Funkcionira vrlo jednostavno -- mi postavimo pitanje, ti odgovoriš najbolje što znaš :)

%h1 Zdravo :)

- if @quizzes.any?

  = simple_form_for @game_submission, url: game_path do |f|

    %section.quizzes
      %p Odaberi kviz:
      = f.input :quiz_id, collection: @quizzes, as: :radio_buttons, label: false

    %section.players
      %p U koliko igrača želiš igrati?
      = f.input :players_count, collection: [["1 igrač", 1], ["2 igrača", 2]], as: :radio_buttons, label: false

    %section.login
      %p Daj drugom igraču da se prijavi:
      %p= f.error :players_credentials
      = f.simple_fields_for :players_credentials, index: nil do |p|
        = p.input :username, input_html: {value: (params[:game_submission][:players_credentials].first[:username] rescue nil)}
        = p.input :password

    = buttons(f) do |b|
      = b.button_button raw("Započni kviz#{content_tag :span, "", class: "name"}!")

- else

  %p Nema još kvizova za igrati.
