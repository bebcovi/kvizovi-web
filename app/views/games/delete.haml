- @title = "Prekid igre"

%h1 Jeste li sigurni?

%p Jeste li sigurni da želite prekinuti igru?

= form_tag game_path, method: :delete do

  = hidden_field_tag :interrupted, params[:interrupted]

  = buttons do |b|
    = b.cancel_button "Nisam", edit_game_path
    = b.submit_button "Jesam"
