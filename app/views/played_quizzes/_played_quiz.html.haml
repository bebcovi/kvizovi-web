%h1= played_quiz.quiz.name

- PlayedQuestions(played_quiz) do |question, answer, feedback, idx|
  .l-played{class: "l-question-#{question.category}"}
    - if answer.nil? and played_quiz.question_answers.index(&:nil?) == idx
      %p.text-error (Ovdje je učenik odustao od kviza)

    %h2.question-title= question.content

    = render "played_quizzes/answer/#{question.category}", {question: question, answer: answer}

    - case feedback
    - when 1
      .text-success
        = icon("checkmark-circle-full")
        Točan odgovor
    - when -1
      .muted.space-half-bottom
        (Nije odgovoreno)
      .text-error
        = icon("cancel-circle-full")
        Pogrešan odgovor
    - when 0
      .text-error
        = icon("cancel-circle-full")
        Pogrešan odgovor
