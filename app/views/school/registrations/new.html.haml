- title "Registracija"

= simple_form_for @school, url: registration_path do |f|

  = f.input :name, placeholder: %(Npr. "Osnovna škola Antuna Gustava Matoša") , hint: "Napišite puno ime škole, bez grada (osim ako je on dio naziva)."
  = f.input :level, collection: School::LEVELS, prompt: "Izaberite..."
  = f.input :place
  = f.input :region, collection: regions, prompt: "Izaberite..."
  = f.input :email
  = f.input :username
  = f.input :password
  = f.input :password_confirmation
  = f.input :key, as: :password, hint: "Ovo će vaši učenici koristiti za registraciju".append_icon("arrow-up")

  = buttons(f) do |b|
    = b.cancel_button "Odustani", login_path
    = b.submit_button "Registriraj se"
