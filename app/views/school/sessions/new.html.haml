- title "Prijava"

%h1 Prijava škole

= simple_form_for @login, url: login_path do |f|

  = f.input :username, input_html: {autofocus: current_page?(action: "new")}
  = f.input :password,
    hint: "Zaboravili ste lozinku? #{link_to "Zatražite novu", new_password_reset_path}."
  = f.input :remember_me, as: :boolean, label: false, inline_label: "Zapamti me"

  = buttons(f) do |b|
    = b.cancel_button "Odustani", root_url(subdomain: false)
    = b.submit_button "Prijava"
    %span Nemate korisnički račun? #{link_to "Registrirajte se", new_registration_path}.
