- title "Uređivanje profila"

= simple_form_for @user, url: account_profile_path do |f|

  = f.input :name
  = f.input :username
  = f.input :email
  = f.input :level, collection: levels, include_blank: false
  = f.input :place
  = f.input :region, collection: regions, include_blank: false
  = f.input :key

  = buttons do
    = cancel_button "Odustani", account_profile_path
    = submit_button "Spremi"
