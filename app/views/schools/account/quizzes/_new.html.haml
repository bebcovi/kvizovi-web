= simple_form_for [:account, @quiz], remote: true do |f|

  .row
    .col-sm-6
      = f.input_field :name, class: "input-large"
    .col-sm-6
      = f.button :button, "Napravi kviz".prepend_icon("plus"), class: "btn-primary"
