- title "Uređivanje kvizova"

.m-bottom
  = add_button "Novi kviz", new_account_quiz_path

#quizzes
  %table.table.table-striped#quiz-list
    %tbody
      = render @quizzes if @quizzes.any?

  - if @quizzes.empty?
    .no-items.well Trenutno nemate kvizova.
