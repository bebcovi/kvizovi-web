- title "Uređivanje kvizova"

.space-bottom
  = add_button "Novi kviz", new_account_quiz_path, remote: true

%ol.list-pretty#quizzes
  - if @quizzes.any?
    = render @quizzes
  - else
    %p.no-items
      Trenutno nemate kvizova.
