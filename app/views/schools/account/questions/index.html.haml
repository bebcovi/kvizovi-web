- title @quiz.name

%h1= @quiz.name

.clearfix.m-bottom
  .dropdown.pull-left
    = add_button "Novo pitanje".append_icon("caret-down"), "#", class: "btn btn-primary dropdown-toggle", data: {toggle: "dropdown"}

    %ul.dropdown-menu
      %li= link_to "Točno/netočno".prepend_icon("checkbox"),     new_account_quiz_question_path(category: "boolean")
      %li= link_to "Ponuđeni odgovori".prepend_icon("list"),     new_account_quiz_question_path(category: "choice")
      %li= link_to "Asocijacija".prepend_icon("shuffle"),        new_account_quiz_question_path(category: "association")
      %li= link_to "Upiši točan odgovor".prepend_icon("pencil"), new_account_quiz_question_path(category: "text")

  .pull-right
    .pull-left.m-right-hl
      = render "account/quizzes/activation_button", quiz: @quiz
    .pull-left
      .btn-group
        = button icon("sort"), order_account_quiz_questions_path(@quiz), class: "btn-sm btn-default", title: "Izmijeni redoslijed"
        = edit_button "", edit_account_quiz_path(@quiz), class: "btn-sm", title: "Izmijeni"

      = delete_button "", account_quiz_path(@quiz), class: "btn-sm", title: "Izbriši", data: {confirm: "Jeste li sigurni da želite izbrisati ovaj kviz?"}

- if not @quiz.activated?
  .alert.alert-warning
    Ovaj kviz je trenutno neaktivan, što znači da mu učenici neće moći pristupiti.
    Kada ste dovršili kviz, aktivirajte ga klikom na lampicu gore (#{icon("lamp")}).

- if questions.any?

  #question-list.list-group
    = render partial: "question", collection: questions

- else

  %p.no-items Ovaj kviz trenutno nema pitanja
