- title "Admin"

%h2
  Škole
  %span.count= @schools.count

%table.table.table-striped
  %colgroup
    %col.col-25
    %col.col-25
    %col.col-25
    %col

  %thead
    %tr
      %th Naziv škole
      %th.text-center Broj učenika
      %th.text-center Broj kvizova
      %th Zadnja aktivnost

  %tbody
    - @schools.each do |school|
      %tr
        %td= link_to school.name, admin_school_path(school)
        %td.text-center= school.students.count
        %td.text-center= school.quizzes.activated.count
        %td= activity(school.updated_at, school.quizzes.maximum(:updated_at))

%h2
  Učenici
  %span.count= @students.count

%table.table.table-striped
  %colgroup
    %col.col-20
    %col.col-20
    %col.col-20
    %col.col-20
    %col

  %thead
    %tr
      %th Ime i prezime
      %th.text-center Korisničko ime
      %th Škola
      %th.text-center Broj odigranih kvizova
      %th Zadnja aktivnost

  %tbody
    - @students.each do |student|
      %tr
        %td= student.full_name
        %td.text-center= student.username
        %td= student.school.name
        %td.text-center= student.games.count
        %td= activity(student.updated_at, student.games.maximum(:created_at))
