- @title = "Prijava"

= simple_form_for :student, url: login_path do |f|

  = f.input :username, input_html: {value: params[:student].try(:[], :username)}
  = f.input :password
  = f.input :school, as: :grouped_select, collection: @regions, group_method: :schools, prompt: "Izaberite...", input_html: {name: "school_id"}

  = f.button :submit, "Prijava"
  = link_to "Odustani", root_path

%p Ako nemate svoj korisnički račun, možete se registrirati #{link_to "ovdje", new_student_path}.
