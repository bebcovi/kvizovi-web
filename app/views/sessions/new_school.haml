- @title = "Prijava"

= simple_form_for :school, url: login_path do |f|

  = f.input :username, input_html: {value: params[:school].try(:[], :username)}
  = f.input :password
  = f.input :remember_me do
    = check_box_tag :remember_me, 1, params[:remember_me]

  = f.button :submit, "Prijava"
  = link_to "Odustani", root_path

%p Ako nemate svoj korisnički račun, možete se registrirati #{link_to "ovdje", new_school_path}.
