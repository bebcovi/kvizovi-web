- title "Novi kviz"

%h1 Zdravo :)

- if @quizzes.any?

  = simple_form_for @quiz_specification, url: prepare_quiz_path do |f|

    %section.quizzes
      %p Odaberi kviz:
      = f.input :quiz_id, collection: @quizzes, as: :radio_buttons, label: false

    %section.players.js-hidden
      %p Igraš li sam ili još s nekim?
      = f.input :students_count, collection: [["Samo ja", 1], ["Još netko", 2]], as: :radio_buttons, label: false

    %section.login.js-hidden
      %p Daj drugom igraču da se prijavi:
      = f.error(:students_credentials) if f.error(:students_credentials)
      = f.simple_fields_for :students_credentials, index: nil do |p|
        = p.input :username, input_html: {value: (params[:quiz_specification][:students_credentials].first[:username] if params[:action] == "prepare")}
        = p.input :password

    = buttons class: "js-hidden" do
      = submit_button raw("Započni kviz#{content_tag :strong, "", class: "name"}!")

- else

  %p.no-items
    Nema još kvizova za igrati...
