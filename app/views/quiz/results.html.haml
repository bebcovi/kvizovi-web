- title "#{@quiz.name} · Kraj"

%h1 Rezultati

.progress.progress-small.progress-primary
  .bar{style: "width: 100%"}

= render "played_quizzes/played_quiz", {played_quiz: played_quiz}

= buttons do
  = primary_button "Završi kviz", choose_quiz_path, class: "btn btn-primary fade-in"
