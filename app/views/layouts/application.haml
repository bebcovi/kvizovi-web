!!!
%html{lang: 'hr'}

  %head

    %title Lektire · #{@title}
    %meta{charset: 'utf-8'}
    %meta{name: 'robots', content: 'noindex'}
    = favicon_link_tag
    = stylesheet_link_tag 'application'
    = javascript_include_tag 'application'

  %body

    %header{role: 'banner'}

      %h1 Lektire

    %article{role: 'main'}

      - if user_logged_in?
        %p Prijavljeni ste kao #{link_to current_user, current_user}. #{link_to "Odjava", logout_path, method: :delete}

      - flash.each do |name, message|
        %p{class: "flash_#{name}"}= message

      = yield
