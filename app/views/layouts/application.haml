!!!
%html{lang: "hr"}
  %head
    %meta{charset: "utf-8"}
    %meta{:"http-equiv" => "X-UA-Compatibile", content: "IE=edge,chrome=1"}
    %title Lektire · #{@title}
    %meta{name: "robots", content: "noindex"}
    = favicon_link_tag
    = stylesheet_link_tag "application"
    = javascript_include_tag "application"
    = javascript_include_tag "//use.typekit.net/nfo7hcu.js"
    %script try{Typekit.load();}catch(e){}

  %body{class: body_class}
    .page_wrapper
      %header
        %h1.logo= "Lektire".prepend_icon("books")
        = render "log" if logged_in?

      %article.main_content
        - flash.each do |name, message|
          - if name.in?([:notice, :alert])
            %div{class: "flash #{name}"}
              = remove_button
              %p= message
          - elsif name == :notification
            %div{class: "flash #{name}"}
              = simple_form_for current_school, url: notify_school_path(current_school), method: :put do |f|
                = f.button :button, "".prepend_icon("cancel"), class: "remove"
              %p= raw message

        = link_to "Vodič".append_icon("book"), tour_path, class: "tour", target: "_blank"

        = yield

      %footer
        %p Za prijave grešaka, savjete ili nešto treće, slobodno nas #{contact_link "kontaktirajte"} :)
        %p.emails janko.marohnic@gmail.com<br>matija.marohnic@gmail.com
