%p#log
  - if current_user.is_a?(Student)
    Bok, #{link_to current_student.first_name, current_student} | #{logout_button "Odjava", logout_path}
  - else
    Prijavljeni ste kao #{link_to current_school, current_school} | #{logout_button "Odjava", logout_path}
