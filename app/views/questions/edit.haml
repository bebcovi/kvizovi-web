- @title = "Izmjena pitanja"

= back_button "Pitanja", polymorphic_path([@parent, Question])

%h1
  %span.name= @parent.name
  %span.section= @title.downcase

= render partial: "form", locals: {url: polymorphic_path([@parent, @question.becomes(Question)]), klass: "edit"}
