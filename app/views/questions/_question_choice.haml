- input_collection(f) do |c|
  - 4.times do |idx|
    = c.input :data, as: :string, label: false, placeholder: "Opcija #{idx + 1}", input_html: {value: question.provided_answers[idx]},
      hint: ("Ovo će biti točan odgovor".append_icon("arrow-up") if idx == 0)
