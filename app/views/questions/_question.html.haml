.list-pretty-item-actions.dropdown.dropdown-hover
  = link_to icon("chevron-left"), "#", class: "dropdown-toggle", data: {toggle: "dropdown"}
  %ul.dropdown-menu.pull-right
    %li= edit_button "Izmijeni", edit_quiz_question_path(@quiz, question), class: "" if @quiz.school == current_user
    %li= remove_file_button "Izbriši", quiz_question_path(@quiz, question), method: :delete if @quiz.school == current_user
    %li= link_to "Preuzmi", download_quiz_question_path(@quiz, question)

%span.space-quart-right= question_icons(question.category)
%span.list-pretty-item-content= question.content
