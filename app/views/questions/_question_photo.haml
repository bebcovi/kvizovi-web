= f.input :attachment

- if question.attachment.present?
  %img.preview{src: question.attachment.url(:medium)}
- else
  %img.preview
  .placeholder= "".prepend_icon("picture")
  = notice "Slika će se proporcionalno smanjiti na visinu od 250px."

= f.input :data, as: :string, label: "Odgovor"
