= simple_form_for @question, url: [@quiz, @question.becomes(Question)], html: {class: "#{params[:action]} #{@question.category}_question"} do |f|

  = render "fields", {f: f}

  = buttons do
    = cancel_button "Odustani", quiz_questions_path(@quiz)
    = submit_button "Spremi"
