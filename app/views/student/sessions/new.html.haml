- title "Prijava"

%h1 Prijava učenika

= simple_form_for @login, url: login_path do |f|

  = f.input :username, input_html: {autofocus: params[:action] == "new"}
  = f.input :password,
    hint: "Zaboravili ste lozinku? #{link_to "Zatražite novu", new_password_reset_path}."

  = buttons do
    = cancel_button "Odustani", root_url(subdomain: false)
    = submit_button "Prijava"
    %span Nemaš korisnički račun? #{link_to "Registriraj se", new_registration_path}.
