- title "Profil"

%table.profile_info
  %colgroup
    %col.field
    %col.value
  %tbody
    %tr
      %th Ime i prezime
      %td= @student.full_name
    %tr
      %th Korisničko ime
      %td= @student.username
    %tr
      %th Škola
      %td= @student.school.name
    %tr
      %th Razred
      %td= @student.grade
    %tr
      %th Spol
      %td= @student.gender
    %tr
      %th Godina rođenja
      %td= @student.year_of_birth.to_s + "."

.btn-toolbar.edit_profile
  .btn-group
    = edit_button "Izmijeni profil", edit_profile_path
    = change_password_button "Izmijeni lozinku", edit_password_path

%p= delete_profile_button "Izbriši korisnički račun", delete_profile_path
