# encoding: utf-8
require "spec_helper"

describe "Game" do
  describe "#create" do
    before(:each) do
      # login(:student)
      # school = double(quizzes: [build_stubbed(:quiz)])
      # school.quizzes = [build_stubbed(:quiz)]
      # Student.find.school = school
    end

    it "works as expected" do
      # choose "Antika"
    end
  end

  context "in 1 player" do
    it "works as expected" do
    end
  end
end
